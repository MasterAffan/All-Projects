import 'package:flutter/material.dart';

class AppColors {
  // Main colors
  static const primary = Color(0xFF2196F3);     // Material Blue
  static const secondary = Color(0xFF00E5FF);    // Cyan
  static const accent = Color(0xFFFF4081);       // Pink accent
  
  // Background colors
  static const background = Color(0xFF121212);   // Pure dark
  static const surface = Color(0xFF1E1E1E);      // Slightly lighter dark
  static const surfaceLight = Color(0xFF2C2C2C); // Card background
  static const error = Color(0xFFFF5252);        // Error red
  
  // Text colors
  static const onPrimary = Colors.white;
  static const onSecondary = Colors.black;
  static const onBackground = Colors.white;
  static const onSurface = Colors.white;
  static const onError = Colors.white;
  
  // Additional colors
  static const textLight = Color(0xFFE0E0E0);    // Light text
  static const textMuted = Color(0xFF9E9E9E);    // Muted text
  static const divider = Color(0xFF2C2C2C);      // Divider color
  
  // Gradients
  static const gradientStart = Color(0xFF2196F3);
  static const gradientEnd = Color(0xFF1976D2);
} 